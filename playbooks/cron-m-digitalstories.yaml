---
- name: Lister les fichiers dans un répertoire spécifique
  hosts: infomaniak
  gather_facts: no
  tasks:
    - name: Lister les fichiers dans /sites/m.digitalstories.com
      ansible.builtin.shell: php-8.0 /home/clients/bd21b4f6400fa6615dcdc469405ed03b/sites/m.digitalstories.com/cron.php
      register: result

    - name: Afficher les résultats
      ansible.builtin.debug:
        msg: "{{ result.stdout_lines }}"
